<% page_title(@post.title)%>
<div class="header">
  <h2><%= (@post.title) %></h2>
</div>
<div class="full_wrap">
<%= render @post %>

<% if current_admin %>
<%= link_to 'Edit', edit_post_path(@post) %> |
<% end %>
<%= link_to 'Back to Posts', posts_path %>
</div>