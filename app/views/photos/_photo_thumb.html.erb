<div class="full_wrap left">
  <div class="photo_wrap">
    <%= link_to image_tag(photo.photo.url(:small), :alt => photo.title, :title => photo.title), photo_path(photo) %>
    <p class="small_text">
     Caption: <%= h photo.title %>
    </p>
  </div>
</div>